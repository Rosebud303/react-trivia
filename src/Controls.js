// import React and Component to create class

// set up Controls class

// state - questionCount (initialed to 0 to keep track of questions displayed)

// this component should return the following:
  // FORM that holds:
  // * INPUT (so user can update questions displayed)
  //  - onChange => update state of questionCount in Controls to
  //  be set to value from input
  // * BUTTON (FILTER QUESTIONS)
  //  - onClick => take the value from state of Controls to notify App of 
  // change